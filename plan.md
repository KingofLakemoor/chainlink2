1. **Change the way animations are defined for Tailwind CSS v4**. Currently in `src/index.css`, custom animations like `animate-zero-title-flicker` are defined inside `@layer utilities` blocks using `@keyframes` and `.animate-zero-title-flicker { animation: ... }`. Tailwind v4 handles custom animations by defining them inside the `@theme` block using the CSS variable `--animate-*` syntax, like `--animate-zero-title-flicker: zero-title-flicker 2.8s infinite;` along with the `@keyframes zero-title-flicker` definition within the `@theme` block. I need to move the animation definitions for `zero` and `vault` inside the `@theme` block in `src/index.css`.
2. **Move all existing `@layer utilities` animations to `@theme` block**. Replace `.animate-something { animation: something ... }` with `--animate-something: something ...;` and ensure `@keyframes something { ... }` is also within `@theme`.
3. **Verify locally**. Run `npm run dev` and ensure the CSS compiles correctly and `dist/index.css` (or `http://localhost:5173/src/index.css`) contains the proper animation utility classes.
4. **Pre-commit step**. Run pre-commit checks using `pre_commit_instructions` and follow them to make sure all verification tests pass.
