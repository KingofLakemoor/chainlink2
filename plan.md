1. **Update `src/App.tsx` filtering logic**: The frontend code ignores `COLLEGE-BASEBALL` games when the user clicks the "BASEBALL" tab to filter matchups. We need to include `COLLEGE-BASEBALL` in the accepted leagues for the `BASEBALL` sport filter.
2. **Update `src/services/espnScraper.ts` live status description parsing**: The live status description is being parsed to be displayed like "Bottom 3rd" or "Top 3rd" but this parsing is currently only applied if `league === "MLB"`. The `COLLEGE-BASEBALL` league also has innings and its status detail comes as "Bot 3rd", which should be updated similarly.
3. **Run Pre-commit Checks**: Run the pre-commit script to verify our changes.
