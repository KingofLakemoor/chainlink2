1. Update `PrizeAdminPage.tsx` to include `winCondition` (defaulting to 'Current Chain') in the `prizeData` state and the admin form.
2. Update `SidebarProgress.tsx` to include `winCondition` in the `prizeData` state and display it below the sponsor name.
3. Review changes and commit.
